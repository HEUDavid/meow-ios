构建 2026073101 (1.4.0)

• 隧道 TCP/IP 引擎的重大内部重构，提升可靠性。网络栈现在由单一专属任务驱动，不再依赖共享锁——这从根本上消除了过去多次出现的"显示已连接但没有流量"卡死问题，尤其是手机过夜休眠唤醒后的场景。
• 请测试：让 VPN 保持连接过夜，并正常日常使用（锁屏/解锁、Wi-Fi 与蜂窝切换）。如果 VPN 图标还在但网页打不开，请在设置中导出日志并反馈。
• 同时包含上一版的 DNS 修复：配置文件的 dns: 段（nameserver、fallback、fake-ip-filter、nameserver-policy）会被尊重。
